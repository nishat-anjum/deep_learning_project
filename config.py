import os

DOWNLOAD_DIRECTORY = os.path.join(os.path.dirname(os.getcwd()), "audios")
CSV_FILE_PATH = os.path.join(os.getcwd(), "platlist.csv")
WAV_FILE_DIRECTORY = os.path.join(os.path.dirname(os.getcwd()), "wav_files")
AUDIO_SPLIT_DURATION = 15 * 1000
